*.log
*.vim
*.swp
.vagrant/
.golangci-lint/

dist/
mcp.json

# promu build / crossbuild output
/.build/
/.tarballs/
/prometheus-mcp

# Embedded prometheus/docs snapshot, downloaded by the `make docs` target.
cmd/prometheus-mcp/external/docs/

# Grafana dashboards synced into Helm chart at build time (source of truth: grafana/)
charts/prometheus-mcp-server/dashboards/
